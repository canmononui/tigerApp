<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiger_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tiger_app">
        <activity
            android:name=".QrPaymentSlipActivity"
            android:exported="false" />
        <activity
            android:name=".QrPaymentActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".input_phone_number"
            android:exported="false" />

        <activity
            android:name=".money_balance"
            android:exported="false" />

        <activity
            android:name=".finish_loading"
            android:exported="false" />
    </application>

</manifest>